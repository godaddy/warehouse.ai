'use strict';

var routes = require('npm-registry-couchapp/registry/rewrites');

//
// TODO: Import routes from `npm-registry-couchapp` that we care about
// and store them onto a file in `dist/` that we use in our proxy application.
//
